name: test
channels:
  - conda-forge
dependencies:
  # Base
  - python
  - pip

  # Testing
  - pytest
  - pytest-cov
  - codecov

  # Runtime deps needed to import and exercise qp in CI
  - biopython
  - click
  - matplotlib
  - numpy
  - pandas
  - periodictable
  - pyyaml
  - requests
  - scikit-learn
  - scipy
  - tqdm
